document.addEventListener("DOMContentLoaded", function() {
  $('.shopify-section--blog-posts.blog-posts-new').each(function(){
    var selector = $(this).find('article-list .article-item');
    if(selector.length <= 0){
      $(this).remove();
    };
  });
});