document.addEventListener("DOMContentLoaded", function() {
  
  var ahora = new Date();
  var fechaObjetivo = new Date("March 31, 2024 23:59:59");

  if (ahora >= fechaObjetivo) {
    var estilos = document.createElement("style");
    estilos.innerHTML = `
      .variant-a__precio-tachado,
      .units-sold-text,
      .boost-sd__product-price--ofert,
      div#rebuy-smart-cart-announcement-bar,
      button#StickyAddToCart span.loader-button__text b {
        display: none!important;
      }
      .product-meta.product-meta .container-general-prices .container-general-prices__regular-prices,
      .boost-sd__product-price {
        display: block!important;
      }
    `;
    document.head.appendChild(estilos);
  }
});
